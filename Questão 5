#include <stdio.h>

int main() {
    int valor;

    printf("Digite um valor: ");
    scanf("%d", &valor);

    if (valor % 2 == 0) {
        printf("O dobro é: %d\n", valor * 2);
    } else {
        printf("O triplo é: %d\n", valor * 3);
    }

    return 0;
}
